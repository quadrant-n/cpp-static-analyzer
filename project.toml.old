[build-system]
requires = ["setuptools", "setuptools-scm"]
build-backend = "setuptools.build_meta:build_main"

[project]
name = "cpp-static-analyzer"
authors = [
  { name  = "Stefanus Nanami", email  = "stefanus-nanami@capcom.com" },
]
license  = "LICENSE"
readme  = "README.md"
requires-python = ">=3.7"

dependencies = [
  iniconfig==2.0.0,
  packaging==24.0,
  pluggy==1.5.0,
  pytest==8.2.0,
  PyYAML==6.0.1
]
